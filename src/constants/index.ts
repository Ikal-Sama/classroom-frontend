export const DEPARTMENTS = [
    'CS',
    'Math',
    'English',
    'Physics',
    'Biology'
];


export const DEPARTMENTS_OPTIONS = DEPARTMENTS.map((dept) => ({
    value: dept,
    label: dept
}))